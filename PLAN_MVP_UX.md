# PLAN_MVP_UX.md — этапы улучшения MVP-бота (≤ 4 чел·часа каждый)

Этот файл — **рабочий чек‑лист**. Отмечайте этапы по мере прохождения.

## Правила объёма
- Каждый этап должен укладываться в **≤ 4 чел·часа**.
- Если этап не укладывается — режем на подэтапы внутри него, но наружу оставляем 1 этап (чтобы не дробить план).
- После каждого этапа обязателен **тест‑прогон** (ручной проход сценариев ниже).

## Статус этапов
- [x] Этап A — конфликт «план/сегодня/задачи» устранён
  - **Дата**:
  - **Ответственный**:
  - **PR/коммит**:
- [x] Этап B — `/start` короткий, 1 CTA, new/returning разведены
  - **Дата**:
  - **Ответственный**:
  - **PR/коммит**:
- [x] Этап C — `first_touch` целиком (скрининг + 2 ветки + переключение)
  - **Дата**:
  - **Ответственный**:
  - **PR/коммит**:
- [x] Этап D — закрытие дня компактное (без спама сообщений)
  - **Дата**:
  - **Ответственный**:
  - **PR/коммит**:
- [x] Этап E — терминология «инициативы» + честные тексты про «ИИ»
  - **Дата**:
  - **Ответственный**:
  - **PR/коммит**:
- [x] Этап F — навигация + сообщения/реактивация + подсказка перед квалификацией + mini app (точечно)
  - **Дата**:
  - **Ответственный**:
  - **PR/коммит**:
- [x] Этап G — редактирование задач + коучинг + help (уплотнение)
  - **Дата**:
  - **Ответственный**:
  - **PR/коммит**:

---

## Этап A (≤ 4 ч): устранить конфликт «план/сегодня/задачи»
**Что входит**: MUST-1 (развести callback’и/кнопки/алиасы; выровнять названия под действие).

**Зона изменений (файлы)**:
- `src/utils/keyboards.js`
- `src/services/reminder.js`
- `src/bot/handlers/upload.js`
- `src/bot/handlers/today.js`
- `src/bot/handlers/plan.js`

**Тест‑прогон**:
- меню: «Сегодня» → открывается чеклист today
- меню: «План» → открывается экран плана (не today)
- «Добавить задачи»/«Редактировать» ведут в ожидаемые действия
- утреннее сообщение и upload ведут в те же экраны

---

## Этап B (≤ 4 ч): короткий `/start` + единый следующий шаг (new/returning)
**Что входит**: MUST-2.

**Зона изменений (файлы)**:
- `src/bot/handlers/start.js`
- `src/utils/keyboards.js`

**Тест‑прогон**:
- new user `/start`: короткий экран, 1 главный CTA «Начать»
- returning `/start`: короткое «с возвращением» + 1 кнопка к действию

---

## Этап C (≤ 4 ч): `first_touch` целиком (скрининг + 2 ветки + переключение)
**Что входит**: MUST-3 + MUST-4.

**Зона изменений (файлы)**:
- `src/bot/index.js` (подключение сцены)
- новая сцена рядом с `src/bot/scenes/onboarding.js`
- `src/bot/handlers/start.js` (вход в сцену для new user)

**Тест‑прогон**:
- пройти ветку A до финального CTA
- пройти ветку B до финального CTA
- в середине каждого пути нажать «переключить ветку» и убедиться, что нет тупика

---

## Этап D (≤ 4 ч): компактное закрытие дня (сводный UX, без спама сообщений)
**Что входит**: MUST-5.

**Зона изменений (файлы)**:
- `src/bot/handlers/dayClose.js`
- ориентир по паттерну редактирования сообщений: `src/bot/handlers/today.js`

**Тест‑прогон**:
- закрыть день при 1/5/15 задачах
- done → запрос времени; skipped → запрос причины
- итог и перенос работают, сообщений существенно меньше

---

## Этап E (≤ 4 ч): терминология «инициативы» + честные тексты про «ИИ»
**Что входит**: MUST-6 + MUST-7.

**Зона изменений (файлы)**:
- `src/bot/handlers/start.js`
- `src/bot/scenes/onboarding.js`
- `src/bot/handlers/sprints.js`
- `src/bot/handlers/plan.js`
- `src/services/coaching/questionTemplates.js`

**Тест‑прогон**:
- быстрый просмотр ключевых экранов: start, onboarding, sprints, plan
- нигде нет параллельных терминов для initiative
- нигде не обещаем LLM/«ИИ анализ», если это правила

---

## Этап F (≤ 4 ч): навигация + сообщения/реактивация + подсказка перед квалификацией + mini app (точечно)
**Что входит**: SHOULD-9 + SHOULD-8 + SHOULD-10 + SHOULD-14.

**Mini app**:
- допускаются **точечные** UX-правки WebApp, только если это нужно для целостности пути «бот → дашборд → обратно в действие».

**Зона изменений (файлы)**:
- `src/utils/keyboards.js`
- `src/services/reminder.js`
- `src/bot/handlers/plan.js`
- опционально: `webapp/*` (микро‑правки)

**Тест‑прогон**:
- из меню доступны ключевые функции без slash-команд
- каждое напоминание: 1 сообщение = 1 действие (1 CTA)
- перед первой квалификацией есть 1 фраза смысла
- переход в дашборд и возврат в действие не создают «потерялся»

---

## Этап G (≤ 4 ч): редактирование задач + коучинг + help (уплотнение)
**Что входит**: SHOULD-11 + SHOULD-12 + SHOULD-13.

**Зона изменений (файлы)**:
- `src/bot/handlers/plan.js`
- `src/services/coaching/questionTemplates.js`
- `src/bot/handlers/start.js`

**Тест‑прогон**:
- редактирование 10 задач не создаёт 10+ отдельных сообщений
- коучинг конкретнее и честнее (без «как будто ИИ»)
- help/overview двухуровневый и ведёт к действию

